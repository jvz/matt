# Matt's Home Modules

To initialize:

```sh
brew install stow
git clone git@github.com:jvz/matt.git
cd matt
stow git
./manage.sh init
stow external
stow zsh
stow vim
```

To update:

```sh
./manage.sh pull
```
